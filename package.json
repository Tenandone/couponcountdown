{
  "name": "couponcountdown-recharge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "markets:fetch": "node --env-file-if-exists=.env scripts/fetch-markets.mjs",
    "build": "node --env-file-if-exists=.env scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "optimize:images": "node scripts/optimize-images.mjs",
    "lint": "eslint src public/site.js scripts tests/*.mjs",
    "prepare:pages": "node scripts/prepare-pages.mjs"
  },
  "devDependencies": {
    "eslint": "^10.11.0",
    "globals": "^17.12.0",
    "jsdom": "^29.1.1",
    "sharp": "^0.35.4"
  },
  "engines": {
    "node": ">=22"
  }
}
